#!/usr/bin/env bash
# Convenience wrapper so `quota` / `quota json` work from anywhere once this
# dir (or a symlink to this file) is on PATH. See HOW-TO-USE.md.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bun run "$SCRIPT_DIR/../src/cli.ts" "$@"
